<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NFC_System.EventManagementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Event Management">

    <Grid Background="#0F0F17">
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- ── Header ── -->
        <Grid Grid.Row="0" Background="#16161F" Padding="16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="← Back"
                    Click="BackButton_Click"
                    Background="#252532"
                    Foreground="#9094A8"
                    Padding="10,5"
                    CornerRadius="5"
                    FontSize="12"
                    VerticalAlignment="Center"/>

            <TextBlock Grid.Column="1"
                       Text="EVENT MANAGEMENT"
                       FontSize="13" FontWeight="Bold"
                       Foreground="#CDD6F4"
                       CharacterSpacing="80"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- ── Main content: 3 columns ── -->
        <Grid Grid.Row="1" Padding="14,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <!-- Left: form + controls -->
                <ColumnDefinition Width="*"/>
                <!-- Middle: events list + attendance log -->
                <ColumnDefinition Width="380"/>
                <!-- Right: activity log — widened so text doesn't cut off -->
            </Grid.ColumnDefinitions>

            <!-- ══ LEFT PANEL ══ -->
            <Grid Grid.Column="0" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Create event form -->
                <Border Grid.Row="0"
                        Background="#16161F"
                        CornerRadius="10"
                        Padding="14"
                        Margin="0,0,0,10"
                        BorderBrush="#2A2A3D"
                        BorderThickness="1">
                    <StackPanel Spacing="0">

                        <TextBlock Text="NEW EVENT"
                                   FontSize="10" FontWeight="Bold"
                                   Foreground="#45475A"
                                   CharacterSpacing="80"
                                   Margin="0,0,0,12"/>

                        <TextBlock Text="Name" Foreground="#6C7086" FontSize="11" Margin="0,0,0,4"/>
                        <TextBox x:Name="EventNameTextBox"
                                 PlaceholderText="Foundation Day 2025"
                                 Background="#252532"
                                 Foreground="White"
                                 BorderBrush="#3D3D52"
                                 CornerRadius="6"
                                 FontSize="12"
                                 Padding="8,6"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Location" Foreground="#6C7086" FontSize="11" Margin="0,0,0,4"/>
                        <TextBox x:Name="EventLocationTextBox"
                                 PlaceholderText="Main Gymnasium"
                                 Background="#252532"
                                 Foreground="White"
                                 BorderBrush="#3D3D52"
                                 CornerRadius="6"
                                 FontSize="12"
                                 Padding="8,6"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Start  (yyyy-MM-dd HH:mm)" Foreground="#6C7086" FontSize="11" Margin="0,0,0,4"/>
                        <TextBox x:Name="StartDateTimeTextBox"
                                 PlaceholderText="2025-06-15 08:00"
                                 Background="#252532"
                                 Foreground="White"
                                 BorderBrush="#3D3D52"
                                 CornerRadius="6"
                                 FontSize="12"
                                 FontFamily="Consolas"
                                 Padding="8,6"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="End  (yyyy-MM-dd HH:mm)" Foreground="#6C7086" FontSize="11" Margin="0,0,0,4"/>
                        <TextBox x:Name="EndDateTimeTextBox"
                                 PlaceholderText="2025-06-15 17:00"
                                 Background="#252532"
                                 Foreground="White"
                                 BorderBrush="#3D3D52"
                                 CornerRadius="6"
                                 FontSize="12"
                                 FontFamily="Consolas"
                                 Padding="8,6"
                                 Margin="0,0,0,14"/>

                        <Button Content="＋  Create Event"
                                Click="CreateEventButton_Click"
                                Background="#89B4FA"
                                Foreground="#0F0F17"
                                FontWeight="Bold"
                                FontSize="12"
                                HorizontalAlignment="Stretch"
                                Padding="10,8"
                                CornerRadius="7"/>
                    </StackPanel>
                </Border>

                <!-- Event controls -->
                <Border Grid.Row="1"
                        Background="#16161F"
                        CornerRadius="10"
                        Padding="14"
                        BorderBrush="#2A2A3D"
                        BorderThickness="1">
                    <StackPanel Spacing="0">

                        <TextBlock Text="CONTROLS"
                                   FontSize="10" FontWeight="Bold"
                                   Foreground="#45475A"
                                   CharacterSpacing="80"
                                   Margin="0,0,0,12"/>

                        <TextBlock Text="Select an event from the list first"
                                   Foreground="#3D3D52"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"/>

                        <Button Content="▶  Start Attendance"
                                Click="StartAttendanceButton_Click"
                                Background="#A6E3A1"
                                Foreground="#0F0F17"
                                FontWeight="Bold"
                                FontSize="12"
                                HorizontalAlignment="Stretch"
                                Padding="10,8"
                                CornerRadius="7"
                                Margin="0,0,0,6"/>

                        <Button Content="■  End Attendance"
                                Click="EndAttendanceButton_Click"
                                Background="#F38BA8"
                                Foreground="#0F0F17"
                                FontWeight="Bold"
                                FontSize="12"
                                HorizontalAlignment="Stretch"
                                Padding="10,8"
                                CornerRadius="7"
                                Margin="0,0,0,6"/>

                        <Button Content="📋  View Attendance"
                                Click="ViewAttendanceButton_Click"
                                Background="#89DCEB"
                                Foreground="#0F0F17"
                                FontWeight="Bold"
                                FontSize="12"
                                HorizontalAlignment="Stretch"
                                Padding="10,8"
                                CornerRadius="7"
                                Margin="0,0,0,6"/>

                        <Button Content="↺  Refresh"
                                Click="RefreshButton_Click"
                                Background="#252532"
                                Foreground="#9094A8"
                                FontSize="12"
                                HorizontalAlignment="Stretch"
                                Padding="10,8"
                                CornerRadius="7"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ══ MIDDLE PANEL ══ -->
            <Grid Grid.Column="1" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- label -->
                    <RowDefinition Height="*"/>
                    <!-- events list -->
                    <RowDefinition Height="Auto"/>
                    <!-- label -->
                    <RowDefinition Height="180"/>
                    <!-- attendance log -->
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="EVENTS"
                           FontSize="10" FontWeight="Bold"
                           Foreground="#45475A"
                           CharacterSpacing="80"
                           Margin="0,0,0,8"/>

                <Border Grid.Row="1"
                        Background="#16161F"
                        CornerRadius="10"
                        Padding="6"
                        Margin="0,0,0,10"
                        BorderBrush="#2A2A3D"
                        BorderThickness="1">
                    <ListView x:Name="EventsListView"
                              Background="Transparent"
                              SelectionMode="Single"
                              Foreground="#CDD6F4"
                              FontSize="12"
                              DoubleTapped="EventsListView_DoubleTapped">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Display}"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="#CDD6F4"
                                           FontSize="12"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <TextBlock Grid.Row="2"
                           Text="ATTENDANCE LOG"
                           FontSize="10" FontWeight="Bold"
                           Foreground="#45475A"
                           CharacterSpacing="80"
                           Margin="0,0,0,8"/>

                <Border Grid.Row="3"
                        Background="#16161F"
                        CornerRadius="10"
                        Padding="6"
                        BorderBrush="#2A2A3D"
                        BorderThickness="1">
                    <ListView x:Name="AttendanceLogListView"
                              Background="Transparent"
                              Foreground="#6C7086"
                              FontSize="11"
                              FontFamily="Consolas"/>
                </Border>
            </Grid>

            <!-- ══ RIGHT PANEL: Activity log ══ -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="ACTIVITY LOG"
                           FontSize="10" FontWeight="Bold"
                           Foreground="#45475A"
                           CharacterSpacing="80"
                           Margin="0,0,0,8"/>

                <Border Grid.Row="1"
                        Background="#16161F"
                        CornerRadius="10"
                        Padding="6"
                        BorderBrush="#2A2A3D"
                        BorderThickness="1">
                    <ListView x:Name="EventLogListView"
                              Background="Transparent"
                              Foreground="#6C7086"
                              FontSize="11"
                              FontFamily="Consolas">
                        <!-- Item template with text wrapping so long messages never clip -->
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="4,2"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           TextWrapping="Wrap"
                                           FontSize="11"
                                           FontFamily="Consolas"
                                           Foreground="#6C7086"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
