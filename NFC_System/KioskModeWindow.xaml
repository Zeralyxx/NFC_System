<Window
    x:Class="NFC_System.KioskModeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Guard Kiosk Mode">

    <!-- Deep dark background to make the colored status cards pop -->
    <Grid Background="#0A0A0A" Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Minimal Header -->
        <Grid Grid.Row="0" Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <FontIcon Glyph="&#xEA18;" FontSize="24" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock x:Name="KioskHeaderSubtitle" Text="NFC VERIFICATION TERMINAL" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" CharacterSpacing="150" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1" Click="ExitKiosk_Click" Padding="16,10" CornerRadius="8" Background="#1AFFFFFF" BorderThickness="0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE711;" FontSize="14"/>
                    <TextBlock Text="Exit Kiosk" FontWeight="SemiBold" FontSize="13"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Massive Full-Screen Content -->
        <Grid Grid.Row="1" ColumnSpacing="32" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="0.8*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Dynamic Authentication Stage (Status, PIN, or QR) -->
            <Border x:Name="StatusBackgroundBorder" Grid.Column="0" CornerRadius="24" Background="#1A3B82F6" BorderBrush="#303B82F6" BorderThickness="2" Padding="32">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Authentication Progress Indicator -->
                    <StackPanel x:Name="ProgressIndicatorPanel" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,0,0,24">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon x:Name="ProgNfcIcon" Glyph="&#xECCA;" FontSize="16" Foreground="#60A5FA"/>
                            <TextBlock x:Name="ProgNfcText" Text="NFC" FontWeight="Bold" Foreground="#60A5FA"/>
                        </StackPanel>

                        <Rectangle x:Name="ProgLine1" Width="40" Height="2" Fill="#30FFFFFF" Margin="8,0" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon x:Name="ProgPinIcon" Glyph="&#xECA7;" FontSize="16" Foreground="#505050"/>
                            <TextBlock x:Name="ProgPinText" Text="PIN" FontWeight="Bold" Foreground="#505050"/>
                        </StackPanel>

                        <Rectangle x:Name="ProgLine2" Width="40" Height="2" Fill="#30FFFFFF" Margin="8,0" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon x:Name="ProgQrIcon" Glyph="&#xED14;" FontSize="16" Foreground="#505050"/>
                            <TextBlock x:Name="ProgQrText" Text="QR" FontWeight="Bold" Foreground="#505050"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Main Dynamic Content Area -->
                    <Grid Grid.Row="1" VerticalAlignment="Center">

                        <!-- PANEL 1: Standard Status (Idle, Transitions, Granted, Denied) -->
                        <Grid x:Name="StatusPanel" Visibility="Visible">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Viewbox Grid.Row="0" MaxHeight="200" Margin="0,0,0,32">
                                <FontIcon x:Name="StatusIcon" Glyph="&#xE72A;" Foreground="#3B82F6"/>
                            </Viewbox>

                            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Spacing="12">
                                <TextBlock x:Name="StatusHeadlineText" Text="READY TO SCAN" FontSize="56" FontWeight="Black" Foreground="#60A5FA" HorizontalAlignment="Center" TextAlignment="Center"/>
                                <TextBlock x:Name="StatusSubheadlineText" Text="Please tap NFC ID on the reader" FontSize="24" Foreground="White" Opacity="0.8" HorizontalAlignment="Center" TextAlignment="Center"/>

                                <Button x:Name="ForgotIdButton" Content="Forgot ID? Use QR (Press D)" Click="ForgotIdButton_Click" Background="#1AFFFFFF" BorderThickness="1" BorderBrush="#30FFFFFF" Foreground="White" CornerRadius="8" Padding="16,10" Margin="0,24,0,0" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <!-- PANEL 2: Hardware PIN Entry -->
                        <StackPanel x:Name="PinPanel" Visibility="Collapsed" HorizontalAlignment="Center" Spacing="32">
                            <TextBlock Text="ENTER SECURITY PIN" FontSize="32" FontWeight="Black" Foreground="#60A5FA" HorizontalAlignment="Center" CharacterSpacing="100"/>

                            <!-- Masked PIN Dots -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="24" Margin="0,16">
                                <FontIcon x:Name="PinDot1" Glyph="&#xEA3A;" FontSize="40" Foreground="#505050"/>
                                <FontIcon x:Name="PinDot2" Glyph="&#xEA3A;" FontSize="40" Foreground="#505050"/>
                                <FontIcon x:Name="PinDot3" Glyph="&#xEA3A;" FontSize="40" Foreground="#505050"/>
                                <FontIcon x:Name="PinDot4" Glyph="&#xEA3A;" FontSize="40" Foreground="#505050"/>
                            </StackPanel>

                            <TextBlock x:Name="PinErrorText" Text="Incorrect PIN. Attempts remaining: 2" FontSize="20" FontWeight="SemiBold" Foreground="#F87171" HorizontalAlignment="Center" Visibility="Collapsed"/>

                            <StackPanel Spacing="8" Margin="0,24,0,0" Opacity="0.6">
                                <TextBlock Text="Use the terminal keypad to continue." FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                                <TextBlock Text="Press A to Confirm" FontSize="16" FontFamily="Consolas" HorizontalAlignment="Center"/>
                                <TextBlock Text="Press B to Delete" FontSize="16" FontFamily="Consolas" HorizontalAlignment="Center"/>
                                <TextBlock Text="Press C = Abort" FontSize="16" FontFamily="Consolas" HorizontalAlignment="Center"/>
                                <TextBlock Text="Press D = Scan QR" FontSize="16" FontFamily="Consolas" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- PANEL 3: QR Camera Viewport -->
                        <Grid x:Name="QrPanel" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="QR VERIFICATION" FontSize="32" FontWeight="Black" Foreground="#60A5FA" HorizontalAlignment="Center" CharacterSpacing="100" Margin="0,0,0,24"/>

                            <!-- Camera Viewport Base -->
                            <Border Grid.Row="1" CornerRadius="16" Background="Black" BorderBrush="#30FFFFFF" BorderThickness="2" MaxWidth="500" MaxHeight="500" HorizontalAlignment="Center">
                                <Grid>
                                    <Image x:Name="KioskCameraPreview" Stretch="UniformToFill"/>
                                </Grid>
                            </Border>

                            <TextBlock Grid.Row="2" Text="Align your QR code inside the frame." FontSize="20" Foreground="White" Opacity="0.8" HorizontalAlignment="Center" Margin="0,24,0,0"/>
                        </Grid>

                    </Grid>
                </Grid>
            </Border>

            <!-- RIGHT: Student Photo & Details -->
            <Border x:Name="ProfileBorder" Grid.Column="1" CornerRadius="24" Background="#10FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="2" Padding="32" Opacity="0.3">
                <Grid VerticalAlignment="Center">
                    <StackPanel Spacing="16">
                        <Border Width="200" Height="200" CornerRadius="100" Background="#1AFFFFFF" HorizontalAlignment="Center" BorderBrush="#30FFFFFF" BorderThickness="4">
                            <FontIcon Glyph="&#xE77B;" FontSize="90" Foreground="#60FFFFFF"/>
                        </Border>

                        <StackPanel Spacing="6" Margin="0,12,0,0">
                            <TextBlock Text="STUDENT NAME" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" CharacterSpacing="100"/>
                            <TextBlock x:Name="StudentNameText" Text="---" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Rectangle Height="1" Fill="#15FFFFFF" Margin="32,4"/>

                        <StackPanel Spacing="6">
                            <TextBlock Text="STUDENT ID" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" CharacterSpacing="100"/>
                            <TextBlock x:Name="StudentIdText" Text="---" FontSize="26" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#A0A0A0"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>