<Window
    x:Class="NFC_System.RegistrationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RegistrationWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="28" RowSpacing="20" ColumnSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Back to Dashboard" Padding="16,10" Click="BackButton_Click" CornerRadius="12"/>
            <StackPanel Grid.Column="1" Margin="18,0,0,0" Spacing="4">
                <TextBlock Text="Student Enrollment" FontSize="36" FontWeight="Bold" TextWrapping="Wrap"/>
                <TextBlock Text="Register details, set 2FA PIN, and bind NFC/QR credentials" FontSize="16" Opacity="0.72" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="2.2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Form -->
            <Border Grid.Column="0" Padding="24" CornerRadius="24" Background="#1F2A37" BorderBrush="#30FFFFFF" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Identity Information" FontSize="28" FontWeight="SemiBold"/>
                        <TextBox x:Name="StudentIdTextBox" Header="Student ID" PlaceholderText="e.g. 26-00001"/>
                        <TextBox x:Name="FullNameTextBox" Header="Full Name" PlaceholderText="Enter complete name"/>
                        <TextBox x:Name="CourseTextBox" Header="Course" PlaceholderText="e.g. BS Computer Science"/>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" x:Name="YearLevelTextBox" Header="Year Level" PlaceholderText="e.g. 3"/>
                            <TextBox Grid.Column="1" x:Name="SectionTextBox" Header="Section" PlaceholderText="e.g. 3A"/>
                        </Grid>

                        <!-- NEW: PIN Authentication Setup -->
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="StatusComboBox" Grid.Column="0" Header="Status" SelectedIndex="0" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Active"/>
                                <ComboBoxItem Content="Inactive"/>
                                <ComboBoxItem Content="Expelled"/>
                            </ComboBox>
                            <PasswordBox x:Name="PinPasswordBox" Grid.Column="1" Header="Set 4-Digit PIN (2FA)" MaxLength="4" PlaceholderText="****"/>
                        </Grid>

                        <Border CornerRadius="16" Padding="16" Background="#182231" Margin="0,6,0,0">
                            <StackPanel Spacing="10">
                                <TextBlock Text="NFC Enrollment" FontSize="20" FontWeight="SemiBold"/>
                                <TextBox x:Name="NfcUidTextBox" Header="NFC UID" PlaceholderText="Tap NFC card or type UID for testing"/>
                                <TextBox x:Name="QrCredentialTextBox" Header="QR Credential Payload" PlaceholderText="Auto-generated from student ID and NFC UID"/>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,10,0,0" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="1.2*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" x:Name="ScanUidButton" Content="Scan UID" Height="46" CornerRadius="14" HorizontalAlignment="Stretch"/>
                            <Button Grid.Column="1" x:Name="ClearButton" Content="Clear" Height="46" CornerRadius="14" HorizontalAlignment="Stretch"/>
                            <Button Grid.Column="2" x:Name="SaveButton" Content="Save Student" Height="46" CornerRadius="14" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right: Preview / Log -->
            <Grid Grid.Column="1" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.45*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <!-- Preview -->
                <Border Grid.Row="0" Padding="24" CornerRadius="24" Background="#2B2352" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Credential Preview" FontSize="28" FontWeight="SemiBold"/>

                        <Grid Grid.Row="1" ColumnSpacing="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.4*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left: Student Info -->
                            <Border Grid.Column="0" CornerRadius="20" Padding="18" Background="#18000000">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <TextBlock x:Name="PreviewTextBlock" Text="Student information will appear here." TextWrapping="Wrap" FontSize="20" LineHeight="30"/>
                                </ScrollViewer>
                            </Border>

                            <!-- Right: Photo & Generated QR -->
                            <StackPanel Grid.Column="1" Spacing="12">
                                <Border CornerRadius="20" Background="#18000000" Height="150">
                                    <Viewbox Stretch="Uniform" Margin="20">
                                        <SymbolIcon Symbol="Contact"/>
                                    </Viewbox>
                                </Border>
                                <Border CornerRadius="20" Background="#18000000" Height="150">
                                    <!-- This is where ZXing generated QR will go -->
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <FontIcon Glyph="&#xE72B;" FontSize="40" Opacity="0.5"/>
                                        <TextBlock Text="QR Pending" Opacity="0.5" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Logs -->
                <Border Grid.Row="1" Padding="24" CornerRadius="24" Background="#23283A" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="16">
                        <TextBlock Text="System Audit Logs" FontSize="26" FontWeight="SemiBold"/>
                        <ListView x:Name="UidLogListView" MinHeight="160"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
