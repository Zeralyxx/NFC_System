<Window
    x:Class="NFC_System.EventAttendanceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="EventAttendanceWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="28" RowSpacing="20" ColumnSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Back" Padding="16,10" Click="BackButton_Click" CornerRadius="12"/>

            <StackPanel Grid.Column="1" Margin="18,0,0,0" Spacing="4">
                <TextBlock Text="Event Attendance" FontSize="34" FontWeight="Bold" TextWrapping="Wrap"/>
                <TextBlock Text="Validate event eligibility and record attendance through NFC, PIN, and QR flow" FontSize="16" Opacity="0.72" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <ComboBox x:Name="ActiveEventComboBox" Header="Active Event" PlaceholderText="Select event" Width="320" DisplayMemberPath="DisplayName"/>
                <Button x:Name="RefreshEventsButton" Content="Refresh" Padding="16,10" Margin="0,28,0,0" CornerRadius="12"/>
                <TextBox x:Name="EventModeTextBox" Header="Event Risk Mode" Text="-" Width="220" IsReadOnly="True"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid ColumnSpacing="24" MinHeight="680">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.35*"/>
                <ColumnDefinition Width="1.65*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="24" CornerRadius="24" Background="#183A25" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="18">
                        <TextBlock Text="Attendance Decision" FontSize="28" FontWeight="SemiBold"/>
                        <Border x:Name="ResultBorder" CornerRadius="22" Padding="28" Background="#FF444444">
                            <StackPanel HorizontalAlignment="Center" Spacing="10">
                                <TextBlock x:Name="ResultTextBlock" Text="SELECT EVENT" FontSize="34" FontWeight="Bold" HorizontalAlignment="Center" TextAlignment="Center"/>
                                <TextBlock x:Name="ResultSubTextBlock" Text="Enter the active event ID before scanning" FontSize="16" Opacity="0.8" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="SuccessSummaryBorder" CornerRadius="16" Padding="16" Background="#123D2A" BorderBrush="#3034D399" BorderThickness="1" Visibility="Collapsed">
                            <StackPanel Spacing="6">
                                <TextBlock Text="Success Summary" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock x:Name="SuccessSummaryTextBlock" Text="-" FontSize="15" TextWrapping="Wrap" Opacity="0.85"/>
                            </StackPanel>
                        </Border>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ManualUidTextBox" Grid.Column="0" Header="Manual NFC UID" PlaceholderText="Use when reader is offline"/>
                            <Button x:Name="ProcessManualUidButton" Grid.Column="1" Content="Process UID" Height="44" Margin="0,28,0,0" CornerRadius="8"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Padding="24" CornerRadius="24" Background="#1F2A37" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <Grid RowSpacing="18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Student Attendance Details" FontSize="28" FontWeight="SemiBold"/>

                        <Grid Grid.Row="1" ColumnSpacing="16" RowSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="StudentNameTextBlock" Grid.Row="0" Grid.Column="1" Text="-" FontSize="26" FontWeight="Bold" TextWrapping="WrapWholeWords"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Student ID" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="StudentIdTextBlock" Grid.Row="1" Grid.Column="1" Text="-" FontSize="22" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Course" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="CourseTextBlock" Grid.Row="2" Grid.Column="1" Text="-" FontSize="22" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Year / Section" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="YearSectionTextBlock" Grid.Row="3" Grid.Column="1" Text="-" FontSize="20" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Enrollment" Opacity="0.72" FontSize="17"/>
                            <Border x:Name="StatusBadge" Grid.Row="4" Grid.Column="1" Padding="12,6" CornerRadius="999" HorizontalAlignment="Left" Background="#33444444">
                                <TextBlock x:Name="StatusTextBlock" Text="-" FontSize="18" FontWeight="Bold"/>
                            </Border>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Event ID" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="SelectedEventTextBlock" Grid.Row="5" Grid.Column="1" Text="-" FontSize="20" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="NFC UID" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="UidTextBlock" Grid.Row="6" Grid.Column="1" Text="-" FontSize="18" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Timestamp" Opacity="0.72" FontSize="17"/>
                            <TextBlock x:Name="ScanTimeTextBlock" Grid.Row="7" Grid.Column="1" Text="-" FontSize="20" TextWrapping="Wrap"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Column="1" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="24" CornerRadius="24" Background="#23283A" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Event Authentication Steps" FontSize="26" FontWeight="SemiBold"/>

                        <Border CornerRadius="16" Padding="16" Background="#18000000" BorderBrush="#30FFFFFF" BorderThickness="1">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1.4*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Text="Event Risk Mode" FontSize="14" Opacity="0.72"/>
                                    <TextBlock x:Name="EventRiskModeTextBlock" Text="-" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <TextBlock Text="Required Steps" FontSize="14" Opacity="0.72"/>
                                    <TextBlock x:Name="EventRequiredStepsTextBlock" Text="-" FontSize="18" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid ColumnSpacing="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="10">
                                <TextBlock Text="1. PIN Entry" FontSize="18" Opacity="0.8"/>
                                <PasswordBox x:Name="VerifyPinBox" FontSize="24" MaxLength="4" PlaceholderText="Enter PIN" IsEnabled="False" HorizontalAlignment="Stretch"/>
                                <Button x:Name="SubmitPinButton" Content="Submit PIN" HorizontalAlignment="Stretch" CornerRadius="8" IsEnabled="False"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Spacing="10">
                                <TextBlock Text="2. QR Credential" FontSize="18" Opacity="0.8"/>
                                <Border Height="120" CornerRadius="12" Background="#18000000" BorderBrush="#30FFFFFF" BorderThickness="1">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <FontIcon Glyph="&#xE714;" FontSize="30" Opacity="0.5"/>
                                        <TextBlock Text="Camera QR Pending" Opacity="0.5" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                <TextBox x:Name="QrCredentialTextBox" PlaceholderText="Paste scanned QR payload" IsEnabled="False"/>
                                <Button x:Name="SubmitQrButton" Content="Validate QR" HorizontalAlignment="Stretch" CornerRadius="8" IsEnabled="False"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Padding="24" CornerRadius="24" Background="#1A1F2E" BorderBrush="#30FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Event Attendance Log" FontSize="26" FontWeight="SemiBold"/>
                            <Border Grid.Column="1" Padding="10,4" CornerRadius="999" Background="#2234D399">
                                <TextBlock Text="Event Mode" FontWeight="SemiBold" Foreground="#34D399"/>
                            </Border>
                        </Grid>
                        <ListView x:Name="AttendanceLogListView" MinHeight="240"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
        </ScrollViewer>
    </Grid>
</Window>
