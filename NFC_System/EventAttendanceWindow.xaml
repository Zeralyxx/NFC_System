<Window
    x:Class="NFC_System.EventAttendanceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Event Attendance">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="48" Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header & Event Selection -->
        <Grid Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Back to Dashboard" Padding="16,10" Click="BackButton_Click" CornerRadius="{StaticResource ButtonCornerRadius}" VerticalAlignment="Top" Margin="0,4,0,0"/>

            <StackPanel Grid.Column="1" Margin="24,0,0,0" Spacing="4">
                <TextBlock Text="Event Attendance" Style="{StaticResource PageTitleStyle}" Margin="0"/>
                <TextBlock Text="Validate event eligibility and record attendance through NFC, PIN, and QR flow" Style="{StaticResource BodyTextStyle}" FontSize="16"/>
            </StackPanel>

            <!-- Active Event Controls -->
            <Border Grid.Column="2" CornerRadius="12" Background="#0AFFFFFF" BorderBrush="#10FFFFFF" BorderThickness="1" Padding="16" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                    <ComboBox x:Name="ActiveEventComboBox" Header="Active Event" PlaceholderText="Select event..." Width="260" DisplayMemberPath="DisplayName"/>
                    <Button x:Name="RefreshEventsButton" Content="Refresh" Height="32" Margin="0,20,0,0" CornerRadius="{StaticResource ButtonCornerRadius}"/>
                    <TextBox x:Name="EventModeTextBox" Header="Event Risk Mode" Text="-" Width="180" IsReadOnly="True"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4.5*"/>
                <ColumnDefinition Width="5.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column: Decision & Details -->
            <Grid Grid.Column="0" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Attendance Decision Engine -->
                <Border Grid.Row="0" Padding="24" CornerRadius="{StaticResource CardCornerRadius}" Background="{StaticResource CardBackgroundBrush}" BorderBrush="#15FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Attendance Decision" Style="{StaticResource CardTitleStyle}"/>

                        <!-- Primary Decision Banner -->
                        <Border x:Name="ResultBorder" CornerRadius="16" Padding="20" Background="{StaticResource WarningBrush}">
                            <StackPanel HorizontalAlignment="Center" Spacing="4">
                                <TextBlock x:Name="ResultTextBlock" Text="NO ACTIVE EVENTS" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center"/>
                                <TextBlock x:Name="ResultSubTextBlock" Text="Create or activate an event before scanning." FontSize="14" Foreground="White" Opacity="0.9" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="SuccessSummaryBorder" CornerRadius="12" Padding="12" Background="#1A34D399" BorderBrush="#3034D399" BorderThickness="1" Visibility="Collapsed">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Success Summary" Style="{StaticResource DataValueStyle}" Foreground="#34D399"/>
                                <TextBlock x:Name="SuccessSummaryTextBlock" Text="-" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- Manual Entry Fallback -->
                        <Grid ColumnSpacing="12" Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ManualUidTextBox" Grid.Column="0" Header="Manual NFC UID" PlaceholderText="Use when reader is offline"/>
                            <Button x:Name="ProcessManualUidButton" Grid.Column="1" Content="Process UID" Height="44" Margin="0,28,0,0" CornerRadius="{StaticResource ButtonCornerRadius}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Student Attendance Details -->
                <Border Grid.Row="1" Padding="24" CornerRadius="{StaticResource CardCornerRadius}" Background="{StaticResource CardBackgroundBrush}" BorderBrush="#15FFFFFF" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Student Attendance Details" Style="{StaticResource CardTitleStyle}"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <Grid ColumnSpacing="16" RowSpacing="10" Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Style="{StaticResource DataLabelStyle}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StudentNameTextBlock" Grid.Row="0" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="22" TextWrapping="WrapWholeWords" VerticalAlignment="Center"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Student ID" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="StudentIdTextBlock" Grid.Row="1" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Course" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="CourseTextBlock" Grid.Row="2" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Year / Section" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="YearSectionTextBlock" Grid.Row="3" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Enrollment" Style="{StaticResource DataLabelStyle}" VerticalAlignment="Center"/>
                                <Border x:Name="StatusBadge" Grid.Row="4" Grid.Column="1" Padding="12,4" CornerRadius="12" HorizontalAlignment="Left" Background="#1AFFFFFF">
                                    <TextBlock x:Name="StatusTextBlock" Text="-" Style="{StaticResource DataValueStyle}"/>
                                </Border>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Event ID" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="SelectedEventTextBlock" Grid.Row="5" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18"/>

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="NFC UID" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="UidTextBlock" Grid.Row="6" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="16"/>

                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Timestamp" Style="{StaticResource DataLabelStyle}"/>
                                <TextBlock x:Name="ScanTimeTextBlock" Grid.Row="7" Grid.Column="1" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18"/>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right Column: Authentication Steps & Logs -->
            <Grid Grid.Column="1" RowSpacing="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Authentication Steps -->
                <Border Grid.Row="0" Padding="32" CornerRadius="{StaticResource CardCornerRadius}" Background="{StaticResource CardBackgroundBrush}" BorderBrush="#15FFFFFF" BorderThickness="1">
                    <StackPanel Spacing="20">
                        <TextBlock Text="Event Authentication Steps" Style="{StaticResource CardTitleStyle}"/>

                        <!-- Context Indicator -->
                        <Border CornerRadius="12" Padding="20" Background="#0AFFFFFF" BorderBrush="#10FFFFFF" BorderThickness="1">
                            <Grid ColumnSpacing="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Text="Active Risk Mode" Style="{StaticResource DataLabelStyle}"/>
                                    <TextBlock x:Name="EventRiskModeTextBlock" Text="-" Style="{StaticResource DataValueStyle}" FontSize="18" Foreground="{StaticResource PrimaryAccentBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <TextBlock Text="Required Steps" Style="{StaticResource DataLabelStyle}"/>
                                    <TextBlock x:Name="EventRequiredStepsTextBlock" Text="-" Style="{StaticResource DataValueStyle}" FontSize="16" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- 2FA & QR Inputs -->
                        <Grid ColumnSpacing="24" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- PIN -->
                            <StackPanel Grid.Column="0" Spacing="12">
                                <TextBlock Text="1. PIN Entry" Style="{StaticResource DataValueStyle}"/>
                                <PasswordBox x:Name="VerifyPinBox" FontSize="20" MaxLength="4" PlaceholderText="****" IsEnabled="False"/>
                                <Button x:Name="SubmitPinButton" Content="Submit PIN" HorizontalAlignment="Stretch" CornerRadius="{StaticResource ButtonCornerRadius}" IsEnabled="False" Height="40"/>
                            </StackPanel>

                            <!-- QR -->
                            <StackPanel Grid.Column="1" Spacing="12">
                                <TextBlock Text="2. QR Credential" Style="{StaticResource DataValueStyle}"/>
                                <Border Height="120" CornerRadius="12" Background="#0AFFFFFF" BorderBrush="#10FFFFFF" BorderThickness="1">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
                                        <FontIcon Glyph="&#xED14;" FontSize="32" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="Camera QR Pending" Style="{StaticResource BodyTextStyle}" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <TextBox x:Name="QrCredentialTextBox" PlaceholderText="Paste scanned QR payload" IsEnabled="False"/>
                                <Button x:Name="SubmitQrButton" Content="Validate QR" HorizontalAlignment="Stretch" CornerRadius="{StaticResource ButtonCornerRadius}" IsEnabled="False" Height="40"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Event Attendance Log -->
                <Border Grid.Row="1" Padding="32" CornerRadius="{StaticResource CardCornerRadius}" Background="{StaticResource CardBackgroundBrush}" BorderBrush="#15FFFFFF" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Event Attendance Log" Style="{StaticResource CardTitleStyle}" Margin="0"/>
                            <Border Grid.Column="1" Padding="12,4" CornerRadius="12" Background="#1A34D399" VerticalAlignment="Center">
                                <TextBlock Text="Event Mode Active" Style="{StaticResource BodyTextStyle}" FontSize="12" FontWeight="SemiBold" Foreground="#34D399"/>
                            </Border>
                        </Grid>

                        <ListView Grid.Row="1" x:Name="AttendanceLogListView" Background="Transparent" Padding="0,0,16,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>